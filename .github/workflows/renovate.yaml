name: Renovate
permissions:
  contents: read
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:
    inputs:
      dryRun:
        description: 'Dry run (log only, opens no PRs)'
        type: boolean
        default: false
concurrency:
  group: renovate
  cancel-in-progress: false
jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      # A PAT rather than GITHUB_TOKEN: PRs opened by GITHUB_TOKEN do not
      # trigger workflows, so the test job would never run on a bot PR.
      - name: Run Renovate
        uses: renovatebot/github-action@v46.2.0
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
        env:
          RENOVATE_AUTODISCOVER: 'false'
          RENOVATE_REPOSITORIES: '["clincha/ElasticStar"]'
          RENOVATE_ONBOARDING: 'false'
          RENOVATE_REQUIRE_CONFIG: 'required'
          RENOVATE_DRY_RUN: ${{ inputs.dryRun && 'full' || 'null' }}
